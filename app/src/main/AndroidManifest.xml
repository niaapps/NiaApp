<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.niaapplications.niaapp">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.NiaApp">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".AboutActivity"
            android:label="@string/abt_b" />
        <activity
            android:name=".BlogActivity"
            android:label="@string/blog_b" />

        <activity
            android:name=".ContactActivity"
            android:label="@string/contact_b" />

        <activity
            android:name=".PricingActivity"
            android:label="@string/price_b" />

        <activity
            android:name=".PortfolioActivity"
            android:label="@string/port_b" />

    </application>

</manifest>